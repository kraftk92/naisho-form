// NEVER commit secrets. Only generic, public config here.
export const FORM_CONFIG = {
  // swap for Cloudflare-Worker endpoint later
  webhookURL: "https://blackink.app.n8n.cloud/webhook-test/naisho-reservation-test",
  maxDaysOut: 21
};
